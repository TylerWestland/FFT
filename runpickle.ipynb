import pickle
import numpy as np
from IPython.display import Audio

with open('stackoverflow.pickle', 'rb') as data:
    dataset = pickle.load(data)

signal = np.fft.ifft(dataset)
display(Audio(signal.T, rate=48000)